---
description: Basic rules for the project
globs:
alwaysApply: true
---

This is a monorepo, managed with pnpm.

packages/web is the web application, using Vite, React and Mantine UI 8 as the UI framework.
It uses the Atomic Design Methodology for the UI components. Routes are managed with React Router.
Preferred is using types of interfaces in typescript.
