node_modules
.next
.git
.github
docs
tests/*
# `scripts/end-to-end-seed.ts` (type-checked as part of `pnpm build`, though
# only ever executed locally/in CI via Playwright's global setup) imports
# `tests/e2e/constants.ts` and `tests/fixtures/seed.ts`. Keep these files so
# the Docker build's TypeScript check can resolve those imports.
!tests/e2e/constants.ts
!tests/fixtures/seed.ts
playwright-report
test-results
.env*
!.env.test
*.md
