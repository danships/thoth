// Moved to `@thoth/database/schemas` (THOTH-058). Re-exported here so existing
// `@/types/schemas/entities/data-view-query` import sites don't need to change.
export * from '@thoth/database/schemas';
